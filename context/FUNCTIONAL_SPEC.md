# MealBot — Функциональная спецификация

_Актуально на май 2026_

---

## Роли пользователей

| Возможность | Гость | User | Pro (перспектива) | Admin |
|---|---|---|---|---|
| Просмотр рецептов (база) | ✅ | ✅ | ✅ | ✅ |
| ИИ-помощник | ❌ (CTA регистрации) | ✅ лимит 10/день | ✅ лимит 100/день | ✅ без лимита |
| Контекст холодильника в ИИ | ❌ | ✅ | ✅ | ✅ |
| ИИ вне базы рецептов | ❌ | ❌ | ✅ | ✅ |
| Холодильник | ❌ (CTA) | ✅ | ✅ | ✅ |
| Добавить рецепт | ❌ | ✅ | ✅ | ✅ |
| Создать группу | ❌ | ✅ | ✅ | ✅ |
| Загрузка фото/видео | ❌ | ✅ | ✅ | ✅ |
| Избранные рецепты | ❌ | ✅ | ✅ | ✅ |
| Telegram-уведомления | ❌ | ✅ | ✅ | ✅ |
| Кастомный ингредиент (КБЖУ) | ❌ | ❌ | ✅ | ✅ |
| Распознавание чеков | ❌ | ❌ | ✅ | ✅ |

---

## Авторизация

### Способы входа
- **Email + пароль** — с подтверждением кода (Resend)
- **Google OAuth** — через @react-oauth/google; auto-link к существующему email-аккаунту
- **SMS** — номер телефона + одноразовый код

### Безопасность
- JWT с `tokenVersion` — server-side invalidation при смене пароля/выходе
- loginLimiter: 10 попыток / 15 мин / email
- Коды верификации: crypto.randomInt, TTL, one-use
- Единый error middleware: Prisma-ошибки не утекают в продакшне

### Привязка Telegram
- Пользователь нажимает "Подключить Telegram" в профиле
- Бэкенд генерирует `pendingTelegramLink` токен (10 мин, rate limit 1/мин)
- Открывается бот с deep link `/start link_TOKEN`
- Бот привязывает `telegramId` к веб-аккаунту
- При наличии отдельного бот-аккаунта: мигрируются FridgeItems и MealPlan

### "Открыть приложение" из бота
- Пользователь нажимает 🌐 в боте
- Бот генерирует `webLoginToken` (одноразовый, 10 мин)
- Открывается `/auth/tg?token=` → выдаётся JWT → редирект на главную

### Бэклог
- "Войти через Telegram" на AuthPage — требует дополнительной проработки flow

---

## API / Производительность

### Пагинация
- `GET /api/dishes` — limit/offset, по умолчанию 20 блюд за запрос
- Ответ: `{ dishes, total, limit, offset }`
- Фронтенд: infinite scroll (IntersectionObserver, подгрузка при прокрутке)

### Rate limits
| Эндпоинт | Лимит | Тип |
|---|---|---|
| Весь `/api` | 100 запросов/мин | express-rate-limit (IP) |
| `/api/chat` | 20 запросов/мин | express-rate-limit (IP) |
| `/api/auth/login` | 10 попыток / 15 мин | express-rate-limit |
| Verify-эндпоинты | 5 попыток / 15 мин | express-rate-limit |
| ИИ-чат (гость) | 2 сообщения/день | in-memory по IP |
| ИИ-чат (user) | 50 сообщений/день | по DB |
| Инвайты | 10/день отправитель, 3/день получатель | по DB |
| `POST /api/comments` | 30/час | по DB |

### Валидация (Zod)
Все write-эндпоинты защищены `validate(schema)` middleware:
- auth: `authRegister`, `authLogin`
- groups: `groupCreate`, `groupUpdate`
- dishes: `dishCreate`, `dishUpdate`, `dishBulk`
- invites: `inviteCreate`
- comments: `commentCreate`
- fridge: `fridgeItemAdd`, `fridgeBulk`, `fridgePatch`
- meal-plans: `mealPlanCreate`

---

## Рецепты и блюда

### Структура рецепта
- Название (nameRu)
- Описание
- Категория: BREAKFAST / LUNCH / DINNER / SNACK / SOUP / SALAD / DESSERT / DRINK
- Время приёма пищи (mealTime): breakfast / lunch / dinner / snack
- Кухня (cuisine)
- Теги (вегетарианское, быстро, без глютена и т.д.)
- Время приготовления (минуты)
- Сложность: easy / medium / hard
- **КБЖУ** — рассчитывается автоматически из ингредиентов (nutrition.js)
- **Ингредиенты с количеством** (amountValue + unit + toTaste + optional)
- **Пошаговый рецепт** (markdown текст)
- **Фото** — до 10 штук, Supabase Storage
- **Видео** — загрузка, Supabase Storage
- Автор (null = системное блюдо, видимость PUBLIC)
- Видимость: PRIVATE / PUBLIC / FAMILY / ALL_GROUPS

### Видимость рецептов
- **PUBLIC** — все пользователи и гости
- **PRIVATE** — только автор
- **FAMILY** — только участники FAMILY-группы автора
- **ALL_GROUPS** — участники всех групп автора

### Форма создания рецепта (два режима)
**Быстрый режим** — минимум полей для быстрого добавления:
- Название *, Категории, Время приёма пищи, Ингредиенты, Видимость
- Видимость по умолчанию: FAMILY
- Категории автовыводятся из mealTime если не выбраны вручную

**Расширенный режим** — все поля:
- Дополнительно: Описание, Кухня, Сложность, Время (мин), Теги, Фото, Видео, Рецепт (шаги), Группа
- При редактировании существующего рецепта открывается всегда в расширенном

### Избранные рецепты
- Кнопка ❤️ на странице блюда (для авторизованных)
- Вкладка «Избранное» в каталоге блюд
- Хранится в таблице `Favorite` (userId + dishId, уникальная пара)

---

## Холодильник

### Текущее состояние
- Добавление из списка (поиск с автодополнением, fuzzy-поиск)
- Быстрый выбор популярных продуктов
- Группировка по категориям
- Количество: quantityValue + quantityUnit у каждого продукта
- Удаление продуктов
- **Семейный холодильник**: общий для всех участников FAMILY-группы
- Автомиграция личного холодильника → семейный при вступлении в FAMILY
- `isBasic=true` ингредиенты (соль, вода, масло...) не учитываются при фильтрации
- `toTaste=true` ингредиенты не учитываются при подборе блюд
- Переключатель 🧊 в хедере: фильтрует главную страницу по наличию в холодильнике
- Для гостей: блок-заглушка с CTA регистрации

### Бэклог
- Срок хранения / напоминания о протухших продуктах
- Управление isBasic (вкл/выкл "всегда есть")
- Pro: добавление через фото чека

---

## ИИ-помощник

- Чат с Claude API
- Контекст: продукты в холодильнике + список блюд из БД
- ИИ выбирает блюда из базы, ставит маркер `[DISH:id]`
- Фронтенд парсит маркеры → inline карточки блюд в чате
- PRO/ADMIN: могут получать рецепты вне базы
- Быстрые подсказки (кнопки)
- История чата для авторизованных
- Гости: ИИ недоступен, сразу CTA регистрации

---

## План питания

- Добавление блюда в план с указанием даты и типа приёма пищи (BREAKFAST / LUNCH / DINNER / ANYTIME)
- Просмотр своего плана + семейного (если в FAMILY-группе)
- Удаление записей из плана
- **Семейный план**: при добавлении/удалении блюда участники семьи получают Telegram-уведомление

---

## Telegram-уведомления

### Семейные события (real-time)
- Участник добавил блюдо в семейный план → все остальные участники получают уведомление
- Участник удалил блюдо из семейного плана → аналогично

### Ежедневное предложение (планировщик)
- Запускается ежечасно, проверяет пользователей у которых сейчас 16:00 по местному времени
- Условия: не открывал приложение сегодня + нет плана на сегодня + ≥5 блюд в личной кухне
- Отправляет 3 случайных блюда из личной кухни/семейной группы
- Не чаще 1 раза в день

### Напоминание о холодильнике
- Условие: холодильник пуст ИЛИ последнее обновление >3 дней назад
- Не чаще 1 раза в 3 дня

### Поля в модели User
- `timezone` — таймзона пользователя (default: Europe/Moscow)
- `lastActiveAt` — обновляется при каждом GET /auth/me
- `lastDailySuggestSentAt` — дата последнего ежедневного предложения
- `lastFridgeReminderSentAt` — дата последнего напоминания о холодильнике

---

## Telegram-бот

### Текущее состояние
- Меню: Завтрак / Обед / Ужин / Перекус / Холодильник / 🌐 Открыть приложение
- Холодильник: просмотр, добавление (текстовые команды, fuzzy-поиск)
- **ИИ-чат: временно скрыт** (кнопка убрана из меню, код закомментирован)
- Рецепты по кнопке, случайное блюдо
- Привязка к веб-аккаунту (deep link)
- `/start`: подтверждение если сессия активна
- Автоочистка токенов при старте

### Бэклог
- Инлайн-поиск блюд
- Управление избранными из бота

---

## Группы

| | FAMILY | REGULAR |
|---|---|---|
| Максимум на пользователя | 1 | 2 |
| Максимум участников | 10 | 1000 |
| Общий холодильник | ✅ | ❌ |
| Общие рецепты | ✅ | ✅ |
| Общий план питания | ✅ | ❌ |
| Приглашение по email | ✅ только owner | ✅ любой участник |
| Вступление по коду | ❌ | ✅ (TTL 7 дней) |

> **REGULAR-группы временно отключены** (UI скрыт, backend возвращает 503/400). Код сохранён.

- Роли: OWNER / MEMBER
- **Приглашение по email**: one-time token, TTL 7 дней, one-use
  - FAMILY: только owner может приглашать; при accept проверяется email получателя
  - Rate limit: 3 инвайта / день / email-получатель, 10 / день / отправитель
  - Отправка через Resend; заглушка (console.log) если RESEND_API_KEY не задан
- При выходе/исключении из FAMILY: продукты возвращаются в личный холодильник
- При удалении FAMILY: все участники получают продукты обратно

---

## КБЖУ

- Поля у `Ingredient`: protein, fat, carbs, avgWeightG, isBasic
- Поля у `DishIngredient`: amountValue, unit, toTaste
- Расчёт при сохранении блюда (`utils/nutrition.js`)
- Отображение на странице блюда

### Бэклог
- Фильтр по калориям в каталоге
- Кастомный ингредиент — Pro
- Рационы на день + расчёт суммарного КБЖУ — Pro

---

## Медиафайлы

- Загрузка фото и видео через форму рецепта
- Хранение: Supabase Storage
- Форматы: JPEG, PNG, WebP (до 5MB); MP4 (до 100MB)
- Фото: до 10 штук на рецепт, выбор главного

---

## Монетизация (перспектива)

- **Pro подписка**: кастомные ингредиенты, рационы, КБЖУ суммарный, ИИ без лимитов
- **Pro фичи**: распознавание чеков (OCR), распознавание фото холодильника
- Поле `subscriptionUntil` в User уже есть в схеме

---

## Логирование

Библиотека: **pino** + **pino-pretty** (только dev).

### Переменные окружения

| Переменная | Prod | Dev |
|---|---|---|
| `NODE_ENV` | `production` | `development` (или не задавать) |
| `LOG_LEVEL` | `info` | `debug` (или не задавать) |

### LOG_LEVEL — допустимые значения (от тихого к подробному)

| Значение | Что логируется |
|---|---|
| `silent` | Ничего |
| `fatal` | Только критические падения |
| `error` | Ошибки + fatal |
| `warn` | Предупреждения + error + fatal |
| `info` | Бизнес-события + warn + error + fatal ← **prod** |
| `debug` | Всё выше + детали ← **dev only** |
| `trace` | Абсолютно всё |

### NODE_ENV — эффект

| Значение | Формат логов | detail в 500-ошибках |
|---|---|---|
| `production` | Чистый JSON в stdout | ❌ не показывается клиенту |
| `development` / любое другое | pino-pretty (цветной) | ✅ виден в ответе |

### Что логируется

- **Auth**: registration, login success/failed, email sent/failed, verification
- **Groups**: created, deleted, left, member kicked
- **Invites**: sent, accepted, rate limit hit
- **Fridge**: fridge_item_added, fridge_item_removed
- **Meal plans**: meal_plan_added, meal_plan_removed
- **Comments**: comment_created, comment_deleted
- **AI**: request sent/completed/failed, blocked by filter, limit exceeded
- **HTTP**: все POST/PUT/DELETE; GET только при ошибке или >500ms

### Что никогда не попадает в логи

Пароли, коды верификации, JWT, invite-токены, telegram-токены, полный email (маскируется: `u***@domain.com`), содержимое чата.

---

## Деплой

- VPS Timeweb, IP 194.87.130.215, домен smarussya.ru
- Backend: Node.js + Express + Prisma, PM2, порт 3001
- Frontend: React + Vite, статика через nginx
- БД: Supabase PostgreSQL (Session Pooler, порт 5432)
- Telegram-бот: PM2
- HTTPS: Let's Encrypt
