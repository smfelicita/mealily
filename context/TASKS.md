# Задачи Meality

Всё основное в продакшене. Этот файл — только активный бэклог.
Историю выполненного смотри в git log.

---

## ✅ Сделано (июнь 2026)
- Ребрендинг **MealBot → Meality** (UI, локали, бот, конфиги, deploy). БД/юзер `mealbot`
  и SSH-ключ `mealbot_deploy` НЕ менялись (живут на сервере).
- Переезд на новый прод-сервер **5.42.112.233 (Москва, msk-1-vm-7usl)**; зарубежный
  VPS `194.87.130.215` — только выход для Telegram-трафика бота.
- **Telegram-бот** через SOCKS5-туннель (systemd `tg-tunnel.service`) — обход DPI-блокировки
  `api.telegram.org` на РФ-хостинге. Новый бот `@mealily_bot`.
- **Email: Resend → Unisender Go** (РФ, серверы в РФ — лучше под 152-ФЗ). Домен `mealily.ru`.
  Единый модуль `backend/src/lib/email.js`. Privacy обновлён.
- **Сброс пароля по коду**: `POST /auth/forgot-password` + `/auth/reset-password`.
- **Вход через Telegram** на AuthPage (таб «Telegram» → бот getlink). SMS-логика сохранена.
- Фикс `app.set('trust proxy', 1)` — корректный rate-limit за nginx.
- **Управление ИИ-чатом флагами админки**: `ai.enabled` (веб) и `telegram.commands.aiEnabled`
  (Telegram) уже работают. На FridgePage CTA «что приготовить» скрывается при `ai.enabled=false`.
- **Бот: надёжность polling** — watchdog в коде (обработчик `polling_error` → `process.exit(1)`
  через 2 мин без восстановления, PM2 поднимает заново) + внешний cron `deploy/bot-healthcheck.sh`
  (каждые 5 мин: getMe через прокси + pending_update_count; рестарт при зависании).
- **Бот: кнопка «🤖 ИИ-помощник» убрана** из меню (обработчик оставлен для закэшированных кнопок).
- **Фикс бота: MealType в верхнем регистре** (BREAKFAST/LUNCH/...) — кнопки подбора по времени
  падали с Prisma-ошибкой. Маппинги вынесены в `shared/mealTypes.js`, покрыты unit-тестами.

---

## 🔥 Активные задачи

### Админка — наполнение алиасов (этап A)
- [ ] Экспортировать ингредиенты в CSV, добавить синонимы, импортировать SQL-ом
- [ ] Merge duplicates — склейка дублей в один canonical

---

## ⚖️ Юридическое (152-ФЗ) — до/во время закрытого теста

- [ ] Страницы `/privacy` (политика ПДн) и `/terms` (пользовательское соглашение) —
      черновики написаны, заполнить данные оператора (ФИО/ИНН самозанятого), вычитать
- [ ] Чекбокс согласия при регистрации (email-форма) + строка «продолжая…» для Google/телефона
- [ ] Подать уведомление оператора ПДн в Роскомнадзор (pd.rkn.gov.ru, онлайн, бесплатно) —
      формально требуется ДО начала обработки; штраф за неподачу для физлиц 5–10 тыс. ₽
- [x] Локализация БД: PostgreSQL перенесена с Supabase (Лондон) на ЛОКАЛЬНУЮ базу
      на прод-сервере 5.42.112.233 (Москва). `localhost:5432/mealbot`. Supabase для БД не используется.
- [x] Локализация МЕДИА: `STORAGE_DRIVER="local"` на проде — фото/видео на локальном диске
      сервера (Москва) + раздача через nginx. Supabase Storage не используется.
- [ ] Уведомление РКН о трансграничной передаче (Anthropic/США — сообщения чата; Telegram).
      Email больше НЕ трансграничный — переехал на Unisender Go (РФ).
- [ ] Возможность удалить аккаунт самостоятельно (сейчас только блокировка админом)
- [ ] Выгрузка своих данных (не обязательно на первом этапе)
- [ ] EN-версии /privacy и /terms (после вычитки русских)
- [ ] Показать документы юристу перед публичным запуском

## 📋 Бэклог

### Качество
- [ ] Integration-тесты: auth flow, invite flow, fridge migration, access control
- [x] Unit-тесты чистой логики: phone, messageFilter, nutrition, chatHelpers, mealTypes —
      `backend/tests/`, запуск `cd backend && npm test` (Vitest, 42 теста)
- [x] Дублирование логики бот/бэкенд — вынесено в `shared/` (aiLimit, flags, fridge, mealTypes);
      backend/src/lib/* — тонкие обёртки, бот подключает shared напрямую
- [ ] In-memory кэши (dishCache в chat.js, флаги) — рассинхрон при нескольких PM2-инстансах
- [ ] SMS: подключить реального провайдера, убрать код из логов (сейчас заглушка console.log).
      Альтернатива уже есть — вход через Telegram (бесплатно, без alpha-name). SMS — по необходимости.

### Фичи
- [ ] Список покупок: автогенерация из выбранных блюд на неделю
- [ ] Напоминание о продуктах (примерный срок хранения)

### ИИ-чат
- [ ] Возврат ИИ-помощника в веб: проработка затрат и монетизации (сейчас скрыт флагом)
- [ ] Фильтр сообщений: переделать с blocklist на whitelist по food-ключевым словам
- [x] «Умный бот» на Haiku: холодильник + релевантные блюда в промпте бота,
      отдельный флаг telegram.aiModel (дефолт claude-haiku-4-5), цены — shared/aiPricing
- ВАЖНО: история веб-чата СОЗНАТЕЛЬНО хранится только в памяти вкладки —
  в БД не сохранять (решение Марины, июнь 2026)

### Pro / монетизация
- [ ] Распознавание чеков (OCR + ИИ)
- [ ] Распознавание еды по фото холодильника
- [ ] Порционный калькулятор

### Инфраструктура
- [ ] Анализ нагрузки → см. `context/system-analysis-plan.md`
- [ ] SEO: SSR или prerender для карточек блюд
- [ ] Аналитика (DAU, популярные блюда)
